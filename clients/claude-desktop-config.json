{
  "//": "Copy the relevant entry into your claude_desktop_config.json. The mcp-remote bridge lets Claude Desktop (stdio) reach this HTTP MCP server. Swap the URL for http://<elastic-ip>/mcp to use the deployed server.",
  "mcpServers": {
    "spring-mcp": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "http://localhost:8080/mcp"]
    }
  }
}
